/**
 * 三，自定义（手写） Promise
 *
 *   3.1 定义整体结构
 *   3.2 Promise 构造函数的实现
 *   3.3 Promise.then() / catch() 实现
 *   3.4 Promise  resolve() / reject() 实现
 *   3.5 Promise  all() / race() 实现
 *   3.6 Promise  resolveDelay() / rejectDelay() : 指定延迟执行回调  （拓展）
 *   3.7 function 完整版本
 *   3.8 es6 class 版本
 *
 */

